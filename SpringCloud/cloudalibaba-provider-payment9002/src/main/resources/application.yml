server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8849

management:
  endpoints:
    web:
      exposure:
        include: '*'